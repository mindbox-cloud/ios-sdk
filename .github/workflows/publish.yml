name: SDK publish

on:
  push:
    branches:
      - 'master'
    tags-ignore:
      - '**'

jobs:
  publish:
    runs-on: macos-13
    steps:
    - name: Remove from Cocoapods
      run: |
        set -eo pipefail
      env:
        COCOAPODS_TRUNK_TOKEN: ${{ secrets.COCOAPODS_TOKEN }}
